{
  "buildCommand": "bun run build",
  "outputDirectory": ".output",
  "framework": null,
  "regions": ["sin1"],
  "routes": [
    { "src": "/_build/(.*)", "dest": "/client/_build/$1" },
    { "src": "/(.*)", "dest": "/server/index.mjs" }
  ]
}
